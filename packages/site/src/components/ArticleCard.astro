---
import type { Article, Source } from "@curation-fyi/shared";

interface Props {
  article: Article;
  source: Source | undefined;
}
const { article, source } = Astro.props;

const date = article.published_at.slice(0, 10);
---

<article class="rounded-lg border border-slate-200 bg-white p-4 transition-shadow hover:shadow-md">
  <div class="mb-1 flex flex-wrap items-center gap-2 text-xs text-slate-500">
    <span class="font-medium text-slate-600">{source?.name ?? article.source_id}</span>
    <span>·</span>
    <time datetime={article.published_at}>{date}</time>
    <span class="rounded bg-slate-100 px-1.5 py-0.5 uppercase">{article.language}</span>
  </div>
  <h2 class="text-base font-semibold leading-snug">
    <a href={article.url} target="_blank" rel="noopener" class="hover:underline">
      {article.title}
    </a>
  </h2>
  {article.summary && <p class="mt-1 line-clamp-2 text-sm text-slate-600">{article.summary}</p>}
</article>
